{
  "name": "good-vibes",
  "version": "1.0.0",
  "description": "A five-player deck-building co-op: build a solarpunk garden by day, hold the line at night.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "build": "echo 'No build step: public/ ships verbatim.'",
    "test": "node --test",
    "check": "npx wrangler deploy --dry-run",
    "deploy": "npx wrangler deploy"
  },
  "license": "MIT"
}
